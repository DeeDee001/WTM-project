-- 1. Create Users Table
CREATE TABLE IF NOT EXISTS `users` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `username` VARCHAR(50) NOT NULL UNIQUE,
  `email` VARCHAR(100) NOT NULL UNIQUE,
  `password` VARCHAR(255) NOT NULL,
  `role` ENUM('employer', 'seeker', 'recruiter', 'admin') NOT NULL,
  `profile_pic` VARCHAR(255) DEFAULT NULL,

  -- Employer Profile specific columns
  `company_name` VARCHAR(100) DEFAULT NULL,
  `company_industry` VARCHAR(100) DEFAULT NULL,
  `company_description` TEXT DEFAULT NULL,
  `company_website` VARCHAR(255) DEFAULT NULL,
  `company_logo` VARCHAR(255) DEFAULT NULL,

  -- Job Seeker Profile specific columns
  `resume_path` VARCHAR(255) DEFAULT NULL,
  `headline` VARCHAR(255) DEFAULT NULL,

  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 2. Create Jobs Table (Employer job posts)
CREATE TABLE IF NOT EXISTS `jobs` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `employer_id` INT NOT NULL,
  `title` VARCHAR(100) NOT NULL,
  `category` VARCHAR(100) NOT NULL,
  `description` TEXT NOT NULL,
  `requirements` TEXT NOT NULL,
  `salary_range` VARCHAR(100) NOT NULL,
  `location` VARCHAR(100) NOT NULL,
  `job_type` VARCHAR(50) NOT NULL,
  `deadline` DATE NOT NULL,
  `status` ENUM('active', 'closed') NOT NULL DEFAULT 'active',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`employer_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 3. Create Applications Table (Seekers apply for jobs)
CREATE TABLE IF NOT EXISTS `applications` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `job_id` INT NOT NULL,
  `seeker_id` INT NOT NULL,
  `cover_letter` TEXT NOT NULL,
  `resume_path` VARCHAR(255) NOT NULL,
  `status` ENUM('Submitted', 'Reviewed', 'Shortlisted', 'Rejected') NOT NULL DEFAULT 'Submitted',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`job_id`) REFERENCES `jobs` (`id`) ON DELETE CASCADE,
  FOREIGN KEY (`seeker_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 4. Create Saved Jobs Table (For the seeker's bookmarking feature)
CREATE TABLE IF NOT EXISTS `saved_jobs` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `seeker_id` INT NOT NULL,
  `job_id` INT NOT NULL,
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `unique_save` (`seeker_id`, `job_id`),
  FOREIGN KEY (`seeker_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  FOREIGN KEY (`job_id`) REFERENCES `jobs` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 5. Create Categories Table (Admin-governed job categories)
-- NOTE: jobs.category stays a free-text VARCHAR (employer's job-post form is
-- unaffected). This table is the admin's canonical list; a category is
-- considered "in use" by matching its name against jobs.category, so
-- deletion can be blocked without a schema change to the jobs table.
CREATE TABLE IF NOT EXISTS `categories` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `name` VARCHAR(100) NOT NULL UNIQUE,
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Seed a few starter categories so Manage Categories isn't empty on first run
INSERT IGNORE INTO `categories` (`name`) VALUES
  ('IT'), ('Finance'), ('HR'), ('Marketing'), ('Sales'), ('Engineering');

-- -------------------------------------------------------------------
-- COPY/PASTE THIS BLOCK DIRECTLY INTO phpMyAdmin or MySQL Workbench
-- -------------------------------------------------------------------
USE `career_bridge`;

CREATE TABLE IF NOT EXISTS `categories` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `name` VARCHAR(100) NOT NULL UNIQUE,
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

INSERT IGNORE INTO `categories` (`name`) VALUES
  ('IT'), ('Finance'), ('HR'), ('Marketing'), ('Sales'), ('Engineering');

